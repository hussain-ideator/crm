# Claude Code

This folder holds project-specific settings and custom slash commands
for Claude Code.

- `settings.json` — project settings (allowed tools, model preferences, etc.)
- `commands/` — custom slash commands (`*.md` files become `/foo` commands)

See https://docs.claude.com/en/docs/claude-code for the latest schema.

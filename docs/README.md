# Docs

- `erd.md` — Entity Relationship Diagram (Mermaid). Update when schema changes.
- `api-contract.md` — REST conventions, error shapes, pagination.
- `setup.md` — full local setup walkthrough.
- `wireframes/` — annotated Zoho screenshots and sketches per module.

These are referenced by `.agent-os/specs/` and by your AI agent during planning.
Keep them current.

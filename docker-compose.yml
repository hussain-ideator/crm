# Local services for development.
# Uncomment Redis when you start building background jobs / caching.
# MySQL is also commented out by default because you mentioned MySQL is
# already installed locally — uncomment if you'd rather run it in Docker.

services:
  # mysql:
  #   image: mysql:8.0
  #   container_name: crm-mysql
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_ROOT_PASSWORD: rootpass
  #     MYSQL_DATABASE: crm
  #     MYSQL_USER: crm
  #     MYSQL_PASSWORD: crmpass
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql

  # redis:
  #   image: redis:7-alpine
  #   container_name: crm-redis
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"

# volumes:
#   mysql_data:
